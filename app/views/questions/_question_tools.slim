-if user_signed_in? && current_user.author_of?(@question)
  p= link_to 'Delete question', question_path(@question), method: :delete
  p= link_to 'Edit question', '', class:'edit_question_link'
  =render 'questions/form_question' 